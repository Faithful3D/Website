prefix: ""

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: "de"
    values:
      layout: "de_default"
  - scope:
      path: "fr"
    values:
      layout: "fr_default"
  - scope:
      path: "article"
    values:
      layout: "article"
