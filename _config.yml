url: "https://www.faithful3d.team"
prefix: ""

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: "de"
    values:
      layout: "default_de"
  - scope:
      path: "fr"
    values:
      layout: "default_fr"
  - scope:
      path: "article"
    values:
      layout: "article"
